<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| Here is where you can register web routes for your application. These
| routes are loaded by the RouteServiceProvider within a group which
| contains the "web" middleware group. Now create something great!
|
*/

/*Route::prefix('shop')->group(function () {
    Route::get('/', 'ShopController@index');
});

Route::get('/{lang}/category', 'CategoryController@index')->middleware('auth');
Route::post('/{lang}/category', 'CategoryController@store')->middleware('auth');

Route::get('/{lang}/profile', 'ProfileController@index')->middleware('auth');
Route::post('/{lang}/profile', 'ProfileController@store')->middleware('auth');
*/